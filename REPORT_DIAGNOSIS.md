# Диагностический отчет: Почему не открываются сделки

## 1. Основная причина (Блокировка API)
**Статус**: 🔴 КРИТИЧНО (P0)
**Симптом**: `[10001] Invalid JSON ... 403 ERROR` в логах при запросе баланса.
**Причина**: Ваш IP-адрес заблокирован Bybit Testnet (CloudFront).
**Детали**: Bybit блокирует доступ из определенных стран (США, РФ и др.) к Testnet API. В логах четко видно HTML-ответ от CloudFront с заголовком "403 ERROR: The request could not be satisfied".
**Последствие**: Бот не может получить баланс кошелька. `Available Balance` считается равным `0.0`. Любая попытка открыть сделку блокируется риск-менеджером как "недостаточно средств" (или Risk Limit).

## 2. Вторая причина (Лимиты Риска)
**Статус**: 🟠 ВАЖНО (P1)
**Симптом**: `[REJECTED: Risk Limit] reason=max_position_size_exceeded`
**Причина**: Стратегия `DynamicRiskLeverage` рассчитывает размер позиции агрессивно, а лимит `MAX_POSITION_SIZE` настроен консервативно.
**Детали**:
*   Стратегия пытается открыть позицию на ~1.77 ETH (сумма ~$6000).
*   Ваш лимит `MAX_POSITION_SIZE` в конфиге (`.env` или по умолчанию) составляет **1000 USDT** (или 200 USDT, если вы меняли).
*   **$6000 > $1000**, поэтому сделка отклоняется.

**Почему стратегия так считает?**
В коде стратегии используется формула:
`Количество = Риск% / %ДоСтопа`
Например: `1.5% / 1% = 1.5` (монет). Для ETH это огромная сумма ($5000+). Стратегия не учитывает реальный размер депозита при расчете лота в монетах.

## 3. Рекомендации

### Шаг 1: Обойдите блокировку (VPN)
Вам **необходимо** использовать VPN или прокси с IP-адресом страны, не попадающей под санкции Bybit (например, Нидерланды, Германия, Сингапур). Без этого бот **не сможет торговать**.

### Шаг 2: Настройте Риск-менеджмент
Чтобы разрешить стратегии открывать такие сделки, увеличьте лимит в файле `.env`:
```bash
MAX_POSITION_SIZE=10000.0  # Увеличьте с 1000 до 10000
```
*Или* уменьшите риск в `strategies.yaml` (`type_a_risk`, `type_b_risk` и т.д.).

### Шаг 3: Проверьте баланс
После включения VPN перезапустите бота и убедитесь, что в логах (или диагностике) отображается корректный баланс (не 0.0).

---
*Диагностика проведена на основе логов и симуляции расчета риска.*
